- id: claude-primer-check
  name: claude-primer check
  description: Check if claude-primer generated docs are up-to-date
  entry: claude-primer --check --no-git-check
  language: python
  additional_dependencies: ['claude-primer']
  pass_filenames: false
  always_run: true
  stages: [pre-commit]
