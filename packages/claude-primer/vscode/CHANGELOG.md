# Changelog

## 1.9.0

- Fix: Init command now persists `withReadme` and `gitMode` to `.claude-primer.toml`
- Fix: Config format uses INI `[project]` section matching CLI parser

## 1.8.1

- Initial VS Code extension release
